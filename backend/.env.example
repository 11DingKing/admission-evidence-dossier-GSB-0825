# Backend (Spring Boot) - copy to backend/.env or export in shell
DB_URL=jdbc:postgresql://localhost:5432/admission_dossier
DB_USERNAME=postgres
DB_PASSWORD=postgres
SERVER_PORT=8080
APP_SEED_ENABLED=true
